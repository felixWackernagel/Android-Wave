Wave-Robot-API:

Important changes:

ElementGsonAdapter:
AbstractRobot:
	import org.apache.commons.codec.binary.Base64; -> android.util.Base64


Code changes:

// is supported by later android version
String.isEmpty() -> JavaHelper.isEmptry( String toCheck )

// is supported by later android version
IOException( Exception ) -> JavaHelper.newIOException( Throwable toWrap )


ProcessOne:

Wavelt:
	make more methods and constructors public because i change the package structure.